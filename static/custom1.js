
// jQuery(document).ready(function(){
	// $('.single-item').slick();
	// $('.one-time').slick({
		// dots: true,
		// infinite: true,
		// speed: 300,
		// slidesToShow: 1,
		// adaptiveHeight: true,
		// autoplay: true,
		// autoplaySpeed: 1000,
		// arrows: false,
		// pauseOnHover: true,
// });

// });



/*
$('.one-time').slick({
  dots: true,
  infinite: true,
  speed: 300,
  slidesToShow: 1,
  adaptiveHeight: true
});

*/